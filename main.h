#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>

int di_prints(int in);
int _printf(const char *format, ...);
int our_putchar(char cha);
int di_puts(char *strng);
#endif
